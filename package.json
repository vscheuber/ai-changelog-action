{
  "name": "ai-changelog-action",
  "version": "1.0.7",
  "description": "Reusable GitHub Action that updates CHANGELOG.md Unreleased section using an LLM",
  "main": "scripts/generate.js",
  "private": true,
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@anthropic-ai/sdk": "^0.39.0",
    "openai": "^4.86.1"
  },
  "engines": {
    "node": ">=20"
  }
}
